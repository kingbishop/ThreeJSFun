

class Screen {

    static pixelRatio : number = window.devicePixelRatio
    static width: number = window.innerWidth
    static height: number = window.innerHeight
    static aspectRatio: number = window.innerWidth/window.innerHeight

}

export default Screen